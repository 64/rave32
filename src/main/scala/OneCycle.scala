package mrv

import chisel3._
import chisel3.util._

class OneCycle extends Module {
  // TODO: Use a MemoryIO and instantiate a sim wrapper around the memory that can be loaded later.
  // val io = IO(new Bundle {
  // })
  // val memory =
}
